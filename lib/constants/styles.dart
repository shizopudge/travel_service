import 'package:flutter/material.dart';

class Styles {
  static const header = TextStyle(
    color: Colors.white,
    fontSize: 50,
    fontWeight: FontWeight.bold,
  );

  static const small = TextStyle(
    color: Colors.white,
    fontSize: 20,
    fontWeight: FontWeight.bold,
  );

  static const actionColor = Color.fromARGB(255, 10, 39, 11);

  static const mainColor = Color.fromARGB(255, 237, 143, 61);
}
